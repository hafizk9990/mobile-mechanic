import React from 'react' 
import { Text, View, StyleSheet } from 'react-native' 

const FlashScreen = (navigationProps) => {
    return(
        <React.Fragment> 
            <Text> wow </Text>
            {
                // setTimeout(() => {
                //     navigationProps.navigation.navigate('PreSignUp')
                // }, 2000)
            }
        </React.Fragment>
    );
}
export default FlashScreen
